apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: contract-manager

resources:
  - namespace.yaml
  - secrets.yaml
  - configmap.yaml
  - backend.yaml
  - frontend.yaml
  - ingress.yaml

# Uncomment to use image tags from CI
# images:
#   - name: ghcr.io/OWNER/contract-manager/backend
#     newTag: main
#   - name: ghcr.io/OWNER/contract-manager/frontend
#     newTag: main
