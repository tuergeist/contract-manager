# 2026-05-13

## Auftragsbestätigungen in Contract-Attachments-Liste

User-Request: ABs sollen auch in der Attachments-Liste des Vertrages erscheinen, nicht nur als separate Sektion.

**Backend (`contracts/schema.py`)**: `Contract.attachments`-Resolver erweitert. Iteriert zusätzlich `OrderConfirmation.objects.filter(contract=self)` und appendet als virtuelle `ContractAttachmentType` mit:
- `id = -int(oc.id)` (negativ → kollidiert nicht mit echten Attachments, dient als Synthetic-Flag)
- `category = 'order_confirmation'`
- `download_url = oc.pdf_file.url` (direkter MEDIA-Link, kein `/api/attachments/{id}/download/`)
- `description = "Auftragsbestätigung <Nummer> (<status>)"`

**Frontend (`ContractDetail.tsx`)**: `isSynthetic = Number(attachment.id) < 0` Check. Bei synthetischen Einträgen:
- Note-Edit gated (kein UpdateAttachmentMeta-Mutation-Target)
- Category-Edit gated (Select → static Badge)
- Permalink/Analyze/Delete versteckt
- Download + Preview bleiben sichtbar (downloadUrl funktioniert)

Neue Kategorie `order_confirmation` in `getCategoryLabel/getCategoryColor` (lila Badge).

Backend-Test-Suite 31 OC-Tests grün, tsc clean. **Kein Tag** — User-Iteration läuft noch.
