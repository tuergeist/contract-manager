# 2026-07-02

## Update-Hinweis: Changelog-Modal statt externem Link (2.35.2)

Iteration am Update-Banner aus 2.35.0. Vorher: „Changelog ansehen" verlinkte extern (erst GitHub-Tags, dann `changelogs.json`-Blob). Feedback: beides wenig aussagekräftig.

**Jetzt:** Button öffnet ein Modal (`ChangelogModal.tsx`), das `changelogs.json` von **GitHub-raw** (`raw.githubusercontent.com/.../main/frontend/public/changelogs.json`) lädt und per `isNewer(entry.version, CURRENT_VERSION)` auf Einträge **neuer als die installierte Version** filtert — scrollbar, mit Typ-Badges + Details.

**Entscheidung raw statt gebündeltes `/changelogs.json`:** Die installierte App bündelt nur den Changelog-Stand vom Build-Zeitpunkt — die neuen Versionen fehlen dort per Definition. Also live von GitHub. Verifiziert: raw liefert `access-control-allow-origin: *` → Client-Fetch ohne Proxy ok (Content-Type ist zwar `text/plain`, `r.json()` parst trotzdem).

Render-Muster + `TYPE_CONFIG` aus `AboutPage.tsx` gespiegelt (bewusst dupliziert, klein genug; kein gemeinsamer Extract nötig).

## Entbranding: VSX-Logo entfernt

`frontend/public/vsx-logo.jpg` entfernt (`git rm`). Einzige Code-Referenz war `Sidebar.tsx:272` (`<img src="/vsx-logo.jpg">`) → durch neutrale Wortmarke „Contract Manager" ersetzt. Übrige „VSX"-Treffer im Code sind Domänentext (Angebotsnummer-Label, NetSuite-Beispiele, Testdaten, `vsx.net`-Mails) — kein Logo-Bezug, unangetastet. Rest-Referenz nur noch in `frontend/dist/` (Build-Artefakt, nicht getrackt).

## Hinweis zur Release-Historie

Frühere `git push --tags`-only-Konvention wurde hier aufgeweicht: auf Wunsch mit `git push origin main --tags`. Triggert beide CI-Läufe (main + Tag). 2.35.0/2.35.1/2.35.2 so veröffentlicht.
